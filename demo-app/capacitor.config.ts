import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tolutronics.paystackdemo',
  appName: 'Paystack Demo',
  webDir: 'dist',
};

export default config;
